import { run, ethers } from "hardhat";

async function main() {
  // We get the contract to deploy
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
